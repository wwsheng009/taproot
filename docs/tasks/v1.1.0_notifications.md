# v1.1.0 Phase 2: 通知系统开发任务

## 🎯 目标
构建轻量级、非侵入式的通知系统，支持多种消息类型、自动消失和动画效果。

## 📋 任务清单

### 1. 基础设施
- [x] **Package Init**: 创建 `ui/notify` 包
- [x] **Types**: 定义通知类型和配置
  - `Type`: Info, Success, Warning, Error
  - `Notification`: 包含 ID, Title, Message, Type, Duration
  - `Config`: 全局配置 (位置, 默认持续时间, 最大数量)

### 2. Toast Component (单体通知)
- [x] **Model**: 实现单个 Toast 的 Model
- [x] **Styling**: 根据类型应用不同颜色/图标 (使用 `ui/styles`)
- [x] **Lifecycle**: 
  - 创建时的时间戳
  - 倒计时管理
  - 消失动画状态

### 3. Notification Manager (管理器)
- [x] **Model**: 管理所有活跃的 Toast
- [x] **Queue**: 处理多于最大显示数量的消息
- [x] **Layout**: 堆叠逻辑 (从上到下 vs 从下到上)
- [x] **Update**: 处理 `Tick` 和 `NotificationMsg`

### 4. API & Commands
- [x] **Messages**: `ShowNotificationMsg`
- [x] **Helper Functions**: 
  - `NewInfo(title, msg) Cmd`
  - `NewSuccess(title, msg) Cmd`
  - `NewWarn(title, msg) Cmd`
  - `NewError(title, msg) Cmd`

### 5. 示例与测试
- [x] **Example**: 创建 `examples/notify/main.go`
  - 演示触发不同类型的通知
  - 演示并发通知
- [x] **Tests**: 验证消息队列和生命周期逻辑

## 📝 接口定义预览

```go
package notify

type Type int

const (
    Info Type = iota
    Success
    Warning
    Error
)

type Notification struct {
    ID       string
    Type     Type
    Title    string
    Message  string
    Duration time.Duration
}

// Manager 嵌入到主 Model 中
type Manager struct {
    notifications []Notification
    // ...
}

// 触发命令
func Info(title, msg string) tea.Cmd
```

## 📅 进度追踪
- **开始时间**: 2025-01-31
- **状态**: 完成
