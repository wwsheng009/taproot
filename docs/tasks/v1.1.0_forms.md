# v1.1.0 Phase 1: 表单组件开发任务

## 🎯 目标
构建独立、可复用、强类型的表单组件库，支持验证和复杂输入。

## 📋 任务清单

### 1. 基础设施 (Infrastructure)
- [ ] **Package Init**: 创建 `ui/forms` 包
- [ ] **Validator Interface**: 定义验证器接口
- [ ] **Common Validators**: 实现常用验证器
  - `Required`: 必填
  - `MinLength/MaxLength`: 长度限制
  - `Email`: 邮箱格式
  - `Regex`: 正则表达式
  - `Range`: 数值范围

### 2. TextInput (文本输入)
- [ ] **Base Component**: 实现基础输入框
  - 移植 `ui/dialog/input.go` 核心逻辑
  - 解耦 Dialog 依赖
- [ ] **Features**:
  - `Placeholder`: 占位符
  - `EchoMode`: 密码模式/普通模式
  - `Cursor`: 光标闪烁和移动
  - `Width`: 固定/自适应宽度
- [ ] **Validation**: 集成 Validator 接口
- [ ] **Events**: Focus/Blur 处理

### 3. NumberInput (数字输入)
- [ ] **Base Component**: 基于 TextInput 扩展
- [ ] **Features**:
  - `Min/Max`: 范围限制
  - `Step`: 增量控制 (上下键)
  - `Precision`: 小数精度控制
  - `Format`: 数字格式化显示
- [ ] **Validation**: 自动数字验证

### 4. TextArea (多行文本)
- [ ] **Base Component**: 实现多行输入
- [ ] **Features**:
  - `LineWrap`: 自动换行
  - `LineNumbers`: 行号显示
  - `Scrolling`: 垂直/水平滚动
  - `Cursor`: 多行光标导航
- [ ] **Limits**: 最大行数/字符数限制

### 5. 示例与测试
- [ ] **Examples**: 创建 `examples/forms/main.go` 演示所有组件
- [ ] **Tests**: 单元测试覆盖核心逻辑

## 📝 接口定义预览

```go
// Validator 验证器接口
type Validator func(value string) error

// Input 基础输入接口
type Input interface {
    Value() string
    SetValue(string)
    Focus() tea.Cmd
    Blur()
    Update(tea.Msg) (Input, tea.Cmd)
    View() string
}
```

## 📅 进度追踪
- **开始时间**: 2025-01-31
- **状态**: 准备开始
